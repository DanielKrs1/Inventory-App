<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/materialize.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/scripts/materialize.min.js">M.AutoInit()</script>    
    <link rel="shortcut icon" type="image/jpg" href="/images/thumbsup.jpeg"/>
    <title>Inventory</title>
</head>
<body>
    <div class="container">
        <header>
            <nav>
                <div class="nav-wrapper">
                    <a href="/" class="brand-logo">Stuff Manager</a>
                    <a href="#" data-target="mobile-nav" class="sidenav-trigger"><i class="material-icons">menu</i></a>
                    <ul id="desktop-nav" class="right hide-on-med-and-down">
                        <li><a href="/"><i class="material-icons left">home</i>Home</a></li>
                        <li><a href="/inventory"><i class="material-icons left">list</i>Inventory</a></li>
                        <% if (isLoggedIn) { %>
                            <li><a href="/profile"><i class="material-icons left">person</i> Hello, <%=user.name%></a> </li>
                            <li><a href="/logout" class="btn red">Logout</a></li>                        
                        <% } else { %>
                            <li><a href="/login" class="btn blue">Login</a></li>
                        <% } %>
                    </ul>
                </div>
            </nav>
            <ul id="mobile-nav" class="sidenav">
                 <li><a href="/"><i class="material-icons left">home</i>Home</a></li>
                 <li><a href="/stuff"><i class="material-icons left">list</i>Inventory</a></li>
                 <% if (isLoggedIn) { %>
                    <li><a href="/profile"><i class="material-icons left">person</i> Hello, <%=user.name%></a> </li>
                    <li><a href="/logout" class="btn red">Logout</a></li>                        
                <% } else { %>
                    <li><a href="/login" class="btn blue">Login</a></li>
                <% } %>
            </ul>
        </header>

        <div class="section">
            <h2>Inventory:</h2>
            <table>
                <thead>
                    <tr>
                        <th>Item</th>
                        <th>Quantity</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let i = 0; i < inventory.length; i++) { %>
                    <tr>
                        <td><%= inventory[i].item %></td>
                        <td><%= inventory[i].quantity %></td>
                        <td><%= inventory[i].description %></td>
                        <td>
                            <a class="btn-small waves-effect waves-light" href=<%= "/inventory/item/" + inventory[i].id %> ><i
                                    class="material-icons right">edit</i>Info/Edit</a>
                            <a class="btn-small waves-effect waves-light red" href="/inventory/item/<%= inventory[i].id %>/delete">
                                <i class="material-icons right">delete</i>
                                Delete
                            </a>
                        </td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        <div class="section" id="stuffAdd">
            <h2>Add Item:</h2>
            <form action="/inventory" method="post">
                <div class="row">
                    <div class="input-field">
                        <label for="nameInput">Name</label>
                        <input name="name" type="text" id="nameInput" class="validate" data-length="32" required>
                    </div>
                    <div class="input-field">
                        <label for="quantityInput">Quantity</label>
                        <input name="quantity" type="number" id="quantityInput" class="validate" value=1 required>
                    </div>
                    <div class="input-field">
                        <label for="descriptionInput">Description</label>
                        <input name="description" type="text" id="descriptionInput" data-length="100">
                    </div>
                </div>
                <button class="btn waves-effect waves-light green">
                    <i class="material-icons right">add</i>
                    Add
                </button>
            </form>
        </div>
    </div>
</body>
</html>